function sumElements(numbers) {

    let sum = 0;
    let firstNum = Number(numbers[0])
    let lastNum = Number(numbers.push())

    console.log(lastNum)

    sum = firstNum + lastNum;

    // console.log(sum)
}

sumElements([20, 30, 40])