function addAndSubtract(firstNum, secondNum, thirdNum) {

    function sum(firstNum, secondNum) {
        let result = firstNum + secondNum;
        return result;
    }
    let add = sum(firstNum, secondNum)

    function subtract(thirdNum) {
        return thirdNum
    }
    let sub = subtract(thirdNum);

    console.log(add - sub)
}

addAndSubtract(1,

    17,
    
    30)