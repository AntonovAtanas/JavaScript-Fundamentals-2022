function characterSequence (input){

    let softunii = input[0];

    for (let i = 0; i < softunii.length ; i++ ){
        let letter = softunii[i]
        console.log (letter)
    }
    
}

characterSequence (["softuni"])